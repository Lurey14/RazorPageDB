@page
@model RazorPractica.Pages.CreateModel

@{
    ViewData["Title"] = "Create";
}

@* <h1>Create</h1>

<h4>Tarea</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Tarea.nombreTarea" class="control-label"></label>
                <input asp-for="Tarea.nombreTarea" class="form-control" />
                <span asp-validation-for="Tarea.nombreTarea" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Tarea.fechaVencimiento" class="control-label"></label>
                <input asp-for="Tarea.fechaVencimiento" class="form-control" />
                <span asp-validation-for="Tarea.fechaVencimiento" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Tarea.estado" class="control-label"></label>
                <input asp-for="Tarea.estado" class="form-control" />
                <span asp-validation-for="Tarea.estado" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Tarea.idUsuario" class="control-label"></label>
                <input asp-for="Tarea.idUsuario" class="form-control" />
                <span asp-validation-for="Tarea.idUsuario" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} *@

<div class="cuerpo-pagina">
    <h1 class="titulo-principal">Crear Nueva Tarea</h1>
    <div class="contenedor-formulario">
        <form method="post" class="formulario-tarea">
            <div asp-validation-summary="ModelOnly" class="mensaje-error"></div>
            <div class="campo-formulario">
                <label asp-for="Tarea.nombreTarea" class="etiqueta-formulario"></label>
                <input asp-for="Tarea.nombreTarea" class="input-formulario" />
                <span asp-validation-for="Tarea.nombreTarea" class="mensaje-error"></span>
            </div>
            <div class="campo-formulario">
                <label asp-for="Tarea.fechaVencimiento" class="etiqueta-formulario"></label>
                <input asp-for="Tarea.fechaVencimiento" class="input-formulario" />
                <span asp-validation-for="Tarea.fechaVencimiento" class="mensaje-error"></span>
            </div>
            <div class="campo-formulario">
                <label asp-for="Tarea.estado" class="etiqueta-formulario"></label>
                <input asp-for="Tarea.estado" class="input-formulario" />
                <span asp-validation-for="Tarea.estado" class="mensaje-error"></span>
            </div>
            <div class="campo-formulario">
                <label asp-for="Tarea.idUsuario" class="etiqueta-formulario"></label>
                <input asp-for="Tarea.idUsuario" class="input-formulario" />
                <span asp-validation-for="Tarea.idUsuario" class="mensaje-error"></span>
            </div>
            <div class="acciones-formulario">
                <input type="submit" value="Crear" class="boton-formulario" />
                <a asp-page="Index" class="enlace-volver">Volver a la Lista</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
